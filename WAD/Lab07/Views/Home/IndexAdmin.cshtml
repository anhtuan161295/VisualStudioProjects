@model IEnumerable<Lab07.Models.User>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(Lab07.Languages.Lg.CreateNew, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>

            <td>
                @{
                    var user = Session["User"] as Lab07.Models.User;
                    if (user != null && user.IsAdmin == true)
                    {

                        @Html.ActionLink(@Lab07.Languages.Lg.Edit, "Edit", new { id = item.Username }); @Html.Raw(" |");
                        @Html.ActionLink(@Lab07.Languages.Lg.Details, "Details", new { id = item.Username }); @Html.Raw(" |");
                        @Html.ActionLink(@Lab07.Languages.Lg.Delete, "Delete", new { id = item.Username });

                    }

                    else if (user == null)
                    {
                        @*@Html.ActionLink("Details", "Details", new { id = item.Username });*@
                    }

                }
                @*@Html.ActionLink("Edit", "Edit", new { id = item.Username }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Username }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Username })*@
            </td>
        </tr>
                    }
</table>